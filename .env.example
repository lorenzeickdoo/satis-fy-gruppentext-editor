# ==============================================================================
# FRONTEND ENVIRONMENT VARIABLES (with VITE_ prefix)
# These are embedded in the frontend bundle and visible to users
# ==============================================================================

# Microsoft Azure AD Configuration
# Get these values from your Azure AD App Registration
VITE_AZURE_CLIENT_ID=your-azure-client-id
VITE_AZURE_TENANT_ID=your-azure-tenant-id
VITE_AZURE_REDIRECT_URI=your-redirect-uri
VITE_AZURE_AUTHORITY=https://login.microsoftonline.com/your-tenant-id
VITE_AZURE_GROUP_ID=your-authorized-group-id

# Supabase Configuration
# Note: ANON key is designed for frontend use (secure with Row Level Security)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Application Settings
VITE_APP_TITLE="SATIS&FY Group Text Editor"
VITE_APP_VERSION=1.0.0

# Development bypass for SSO (ONLY FOR DEVELOPMENT! Set to false in production)
VITE_DEV_BYPASS_SSO=false

# ==============================================================================
# BACKEND ENVIRONMENT VARIABLES (WITHOUT VITE_ prefix)
# These are ONLY available to serverless functions and NEVER exposed to frontend
# Set these in Vercel Dashboard under Environment Variables
# ==============================================================================

# OpenRouter API Configuration
# Get your API key from https://openrouter.ai/
# IMPORTANT: Must include "Bearer " prefix
OPENROUTER_API_KEY="Bearer sk-or-v1-your-openrouter-api-key"

# SATIS&FY API Configuration
# Bearer token for SATIS&FY API
API_BEARER_TOKEN="your-api-bearer-token"